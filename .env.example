# Example environment configuration for mangadex-dl-wui-vibed
# Copy this file to .env and adjust values as needed

# Redis Configuration
# Use redis://localhost:6379/0 for local development
# For production with auth: redis://:password@host:6379/0
REDIS_URL=redis://localhost:6379/0

# Directory Paths
CACHE_DIR=/downloads/cache      # Persistent cache for downloaded manga
TEMP_DIR=/tmp/mangadex-wui-vibed      # Temporary task working directories

# Time-to-Live Settings (seconds)
TASK_TTL_SECONDS=3600           # 1 hour - task record expiration
CACHE_TTL_SECONDS=604800        # 7 days - cached file expiration (0 = never expire)

# Worker Configuration
RQ_WORKER_COUNT=3               # Number of concurrent download workers

# Python Version (Docker build only)
PYTHON_VERSION=3.12
